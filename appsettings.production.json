{
  "_Comment": "Production-only overrides. Connection strings, JWT key, and email provider are injected via App Service app settings (or Key Vault references) at deploy time. Do not hardcode them here.",
  "Registration": {
    "Mode": "Closed",
    "_Comment": "Default to Closed for Production deployments. Override via app settings."
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
